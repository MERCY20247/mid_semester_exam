//2. What are JavaScript objects, and how are they different from arrays?

// JavaScript objects are key-value pairs used for structured data with named properties.

//  how are they different from arrays
// Objects have named properties whie arrays have indexed values.

// example of an object
let person = { name: "Mercy", age: 30 };
console.log(person.name); // "Mercy"

// example of an array
let numbers = [1, 2, 3];
console.log(numbers[0]); // 1