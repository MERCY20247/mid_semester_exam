//  a function that takes two numbers. If the numbers are the same, return
// their product. If they are different, return their sum.

function computeNumbers(a, b) {
    return a === b ? a * b : a + b;
}

console.log(computeNumbers(4, 4)); 
console.log(computeNumbers(3, 5)); 
